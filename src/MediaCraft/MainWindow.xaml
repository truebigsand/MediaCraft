<Window x:Class="MediaCraft.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:MediaCraft.Views"
        Title="MediaCraft — FFmpeg 转码工具"
        Width="1240" Height="800"
        MinWidth="1020" MinHeight="640"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource PageBrush}"
        FontFamily="Microsoft YaHei UI, Microsoft YaHei, Segoe UI"
        FontSize="12"
        Foreground="{StaticResource TextBrush}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ═══════════════ 主标签页 ═══════════════ -->
        <TabControl Grid.Row="0" x:Name="MainTabs" Background="Transparent" BorderThickness="0">
            <TabItem Header="转码">
                <views:TranscodeView DataContext="{Binding Transcode}" />
            </TabItem>
            <TabItem Header="队列">
                <views:QueueView DataContext="{Binding QueuePage}" />
            </TabItem>
            <TabItem Header="预设">
                <views:PresetView DataContext="{Binding PresetPage}" />
            </TabItem>
            <TabItem Header="设置">
                <views:SettingsView DataContext="{Binding SettingsPage}" />
            </TabItem>
        </TabControl>

        <!-- ═══════════════ 日志开关 ═══════════════ -->
        <Grid Grid.Row="1" Margin="0,6,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ToggleButton x:Name="LogToggle"
                          Grid.Column="0"
                          Content="显示日志"
                          Padding="10,3"
                          FontSize="11"
                          Cursor="Hand"
                          Checked="OnLogToggleChanged"
                          Unchecked="OnLogToggleChanged" />
            <TextBlock Grid.Column="1"
                       x:Name="LogSummary"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       Style="{StaticResource HintText}"
                       Text="" />
            <Button Grid.Column="2"
                    Content="打开日志目录"
                    Style="{StaticResource LinkButton}"
                    Click="OnOpenLogDirectoryClick" />
        </Grid>

        <!-- ═══════════════ 日志面板 ═══════════════ -->
        <Border Grid.Row="2"
                x:Name="LogPanel"
                Style="{StaticResource CardBorder}"
                Margin="0,6,0,0"
                Padding="6"
                Visibility="Collapsed"
                Height="180">
            <ListBox x:Name="LogList"
                     BorderThickness="0"
                     Background="Transparent"
                     FontFamily="Consolas, Cascadia Mono, Courier New"
                     FontSize="11.5"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Line}" TextWrapping="NoWrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Level}" Value="Warn">
                                            <Setter Property="Foreground" Value="{StaticResource WarnBrush}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="Error">
                                            <Setter Property="Foreground" Value="{StaticResource DangerBrush}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="Debug">
                                            <Setter Property="Foreground" Value="{StaticResource FaintBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- ═══════════════ 状态栏 ═══════════════ -->
        <StatusBar Grid.Row="3" Margin="0,6,0,0" Background="Transparent" BorderThickness="0" BorderBrush="Transparent">
            <StatusBarItem>
                <TextBlock Text="ffmpeg：" Style="{StaticResource HintText}" />
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding FfmpegStatus}" Style="{StaticResource HintText}" MaxWidth="420" TextTrimming="CharacterEllipsis" />
            </StatusBarItem>
            <Separator />
            <StatusBarItem>
                <TextBlock Text="{Binding ProbeStatus}" Style="{StaticResource HintText}" Foreground="{StaticResource AccentBrush}" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding QueueStatus}" Style="{StaticResource HintText}" />
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock x:Name="StatusVersion" Text="v0.3.0" Style="{StaticResource HintText}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
